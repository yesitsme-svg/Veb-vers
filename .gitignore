# архивы версий — не нужны в гите, версии живут в коммитах и релизах
*.zip

# macOS
.DS_Store

# локальная рабочая обвязка (не часть проекта, на прод не едет)
.claude/

# backups/ ХРАНИТСЯ в гите: снапшоты версий и CHANGELOG должны быть видны
# на GitHub и переживать смену машины. На прод не едет (деплой = один index.html).
